import React from 'react';
import { StatusBar as ReactStatusBar } from 'react-native';

const StatusBar = () => {
    return (
        <ReactStatusBar barStyle="light-content" backgroundColor="#222831" />
    );
}

export default StatusBar;